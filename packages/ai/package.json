{
  "name": "@agent-whisperer/ai",
  "description": "model client layer over the Vercel AI SDK, targeting the LiteLLM gateway",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "pnpm exec eslint .",
    "smoke": "doppler run -- tsx scripts/smoke.ts"
  },
  "dependencies": {
    "@ai-sdk/openai-compatible": "^0.2.0",
    "ai": "^4.0.30"
  },
  "devDependencies": {
    "@agent-whisperer/eslint-config": "workspace:*",
    "@agent-whisperer/tsconfig": "workspace:*",
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
