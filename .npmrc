engine-strict=true
auto-install-peers=true
strict-peer-dependencies=false
# pnpm 11 supply-chain policy: rejects packages newer than ~24h. Useful for
# CI on a shared repo, pure friction for a personal learning project.
# Reintroduce a non-zero value when CI lands.
minimum-release-age=0
