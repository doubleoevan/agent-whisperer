{
  "name": "@agent-whisperer/mcp",
  "description": "Model Context Protocol server (stdio) exposing tools that enqueue workflows via the outbox",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "pnpm exec eslint .",
    "dev": "doppler run -- tsx src/index.ts",
    "smoke": "doppler run -- tsx scripts/smoke.ts"
  },
  "dependencies": {
    "@agent-whisperer/config": "workspace:*",
    "@agent-whisperer/database": "workspace:*",
    "@agent-whisperer/domain": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.0.4",
    "@temporalio/client": "^1.11.6",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@agent-whisperer/eslint-config": "workspace:*",
    "@agent-whisperer/tsconfig": "workspace:*",
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
